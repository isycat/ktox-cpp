{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"ktox-cpp: Kotlin \u2192 C++ transpiler. <p>ktox-cpp allows you to write Kotlin code and transpile it to idiomatic C++ header files (<code>.hpp</code>), enabling the use of Kotlin's powerful language features (like classes, null safety, and scope functions) in C++ environments.</p>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Classes and Inheritance: Full support for Kotlin classes, constructors, <code>init</code> blocks, and interfaces.</li> <li>Modern C++ output: Generates idiomatic C++ using <code>std::optional</code>, <code>std::vector</code>, <code>std::unordered_map</code>, and structured bindings.</li> <li>Null Safety: Transpiles Kotlin's null-safety operators (<code>?.</code>, <code>?:</code>, <code>!!</code>) and smart casts (<code>is</code>, <code>!is</code>) into robust C++ checks.</li> <li>Comprehensive Standard Library: Includes C++ implementations for dozens of Kotlin standard library functions:<ul> <li>Collections: <code>map</code>, <code>filter</code>, <code>fold</code>, <code>reduce</code>, <code>groupBy</code>, <code>associateBy</code>, <code>zip</code>, <code>flatten</code>, and more.</li> <li>Scope Functions: <code>let</code>, <code>run</code>, <code>with</code>, <code>apply</code>, <code>also</code>, <code>takeIf</code>, <code>takeUnless</code>.</li> </ul> </li> <li>Operator Overloading: Full support for overloading arithmetic, comparison, and indexing operators.</li> <li>Infix Functions: Supports both standard and user-defined <code>infix</code> functions.</li> <li>Advanced Control Flow: <code>if</code>/<code>when</code> as expressions, and <code>for</code> loops with ranges (<code>..</code>, <code>until</code>, <code>downTo</code>, <code>step</code>).</li> </ul>"},{"location":"#quick-links","title":"Quick Links","text":"<ul> <li>Installation &amp; Usage</li> <li>Language Guide (Basics)</li> <li>Classes &amp; Objects</li> <li>Advanced Features</li> </ul>"},{"location":"#shortcomings-limitations","title":"Shortcomings &amp; Limitations","text":"<p>The following items represent current shortcomings, missing features, or partial support in <code>ktox-cpp</code>.</p> Feature Status Description <code>finally</code> blocks \u26a0\ufe0f Partial Emitted as a comment + trailing code. C++ has no <code>finally</code>; use RAII destructors. <code>inner class</code> \u274c Missing Outer pointer is not captured. Workaround: pass outer object manually. <code>sealed class</code> \u26a0\ufe0f Partial Emitted as abstract class; hierarchy enforcement is not performed at transpilation. <code>try</code> as expression \u274c Missing Not supported; emits <code>/* unsupported */</code>. Restructure to <code>var</code> + <code>try</code> block. Destructuring \u274c Missing <code>val (a, b) = pair</code> emits <code>/* unsupported */</code>. Use C++17 structured bindings manually. Secondary Constructors \u274c Missing Not supported. Workaround: use factory functions. Coroutines \u274c Missing <code>suspend fun</code>, <code>async/await</code> are not supported. SAM conversion \u274c Missing Not supported. <code>is</code> type check \u26a0\ufe0f Partial Requires polymorphic types (virtual functions). Does not work for value types/primitives. <code>val</code> + Mutable Collections \u26a0\ufe0f Partial <code>val m = mutableMapOf()</code> emits <code>const auto</code>, preventing modification. Use <code>var</code>. <code>operator fun set</code> \u26a0\ufe0f Partial Cannot be emitted as <code>operator[]</code> (C++ restriction). Call as method <code>obj.set(k, v)</code>. Named Arguments \u274c Missing Ignored; all arguments are passed positionally. Reified Generics \u274c Missing Not supported."},{"location":"advanced/","title":"Language Guide: Advanced Features","text":"<p>This section covers advanced Kotlin language features and their C++ transpilation.</p>"},{"location":"advanced/#generics","title":"Generics","text":"<p><code>ktox-cpp</code> transpiles Kotlin generics to C++ templates.</p> <p>Kotlin: <pre><code>class Box&lt;T&gt;(val value: T)\n\nfun &lt;T&gt; identity(item: T): T = item\n</code></pre></p> <p>C++: <pre><code>template &lt;typename T&gt;\nclass Box {\npublic:\n    T value;\n    Box(T value) : value(value) {}\n};\n\ntemplate &lt;typename T&gt;\nT identity(T item) {\n    return item;\n}\n</code></pre></p>"},{"location":"advanced/#lambdas-higher-order-functions","title":"Lambdas &amp; Higher-Order Functions","text":"<p>Kotlin lambdas are transpiled to C++ lambdas with a capture-all by reference (<code>[&amp;]</code>).</p> <p>Kotlin: <pre><code>val multiply = { x: Int, y: Int -&gt; x * y }\n\nfun &lt;T&gt; T.let(block: (T) -&gt; Unit) {\n    block(this)\n}\n</code></pre></p> <p>C++: <pre><code>const auto multiply = [&amp;](auto x, auto y) {\n    return x * y;\n};\n\ntemplate &lt;typename T, typename F&gt;\nvoid let(T self, F block) {\n    block(self);\n}\n</code></pre></p>"},{"location":"advanced/#collections-standard-library","title":"Collections &amp; Standard Library","text":"<p><code>ktox-cpp</code> provides a runtime library (<code>ktox-lib.hpp</code>) that maps Kotlin's standard library functions to C++ using <code>std::vector</code> and <code>std::unordered_map</code>.</p> <p>Kotlin: <pre><code>val list = listOf(1, 2, 3)\nval doubled = list.map { it * 2 }\nval evens = list.filter { it % 2 == 0 }\n</code></pre></p> <p>C++: <pre><code>const auto list = std::vector&lt;int32_t&gt;{1, 2, 3};\nconst auto doubled = ktox::map(list, [&amp;](auto it) { return it * 2; });\nconst auto evens = ktox::filter(list, [&amp;](auto it) { return it % 2 == 0; });\n</code></pre></p>"},{"location":"advanced/#exception-handling","title":"Exception Handling","text":"<p>Kotlin's <code>try-catch</code> blocks map to C++ <code>try-catch</code>. Standard Kotlin exceptions are mapped to their <code>&lt;stdexcept&gt;</code> equivalents.</p> Kotlin Exception C++ Exception <code>RuntimeException</code> <code>std::runtime_error</code> <code>IllegalArgumentException</code> <code>std::invalid_argument</code> <code>IllegalStateException</code> <code>std::logic_error</code> <code>IndexOutOfBoundsException</code> <code>std::out_of_range</code> <code>Exception</code> <code>std::exception</code> <p>Kotlin: <pre><code>try {\n    performAction()\n} catch (e: Exception) {\n    println(\"Error: ${e.message}\")\n}\n</code></pre></p> <p>C++: <pre><code>try {\n    performAction();\n}\ncatch (std::exception&amp; e) {\n    println(\"Error: \" + ktox::toString(e.what()) + \"\");\n}\n</code></pre></p>"},{"location":"advanced/#finally-blocks","title":"Finally blocks","text":"<p>Kotlin's <code>finally</code> blocks are transpiled using RAII via <code>ktox::ScopeGuard</code> to ensure they run even if an exception is thrown or a <code>return</code> is executed.</p> <p>Kotlin: <pre><code>try {\n    doWork()\n} catch (e: Exception) {\n    handle(e)\n} finally {\n    cleanup()\n}\n</code></pre></p> <p>C++: <pre><code>{\n    auto __finally = ktox::ScopeGuard([&amp;] {\n        cleanup();\n    });\n    try {\n        doWork();\n    }\n    catch (std::exception&amp; e) {\n        handle(e);\n    }\n}\n</code></pre></p>"},{"location":"advanced/#other-features","title":"Other Features","text":""},{"location":"advanced/#destructuring-declarations","title":"Destructuring Declarations","text":"<p>ktox-cpp supports Kotlin's destructuring declarations using C++17 structured bindings.</p> <p>Kotlin: <pre><code>val (a, b) = p\n</code></pre></p> <p>C++: <pre><code>const auto [a, b] = p;\n</code></pre></p>"},{"location":"advanced/#type-aliases","title":"Type Aliases","text":"<p>Kotlin <code>typealias</code> is transpiled to a C++ <code>using</code> declaration.</p> <p>Kotlin: <pre><code>typealias StringList = List&lt;String&gt;\n</code></pre></p> <p>C++: <pre><code>using StringList = std::vector&lt;std::string&gt;;\n</code></pre></p>"},{"location":"advanced/#noreturn-functions","title":"Noreturn Functions","text":"<p>Functions returning <code>Nothing</code> are marked with the <code>[[noreturn]]</code> attribute in C++.</p> <p>Kotlin: <pre><code>fun fail(): Nothing = throw RuntimeException(\"fail\")\n</code></pre></p> <p>C++: <pre><code>[[noreturn]] void fail() {\n    throw std::runtime_error(\"fail\");\n}\n</code></pre></p>"},{"location":"advanced/#annotations","title":"Annotations","text":"<p>Annotations can be used to control the transpilation process.</p> <p>Kotlin: <pre><code>@NativeName(\"external_func\")\nfun myFunc() { }\n</code></pre></p> <p>C++: <pre><code>// Emitted as:\nvoid external_func() { }\n</code></pre></p>"},{"location":"basics/","title":"Basics &amp; Core Concepts","text":"<p>This section covers the fundamental building blocks of <code>ktox-cpp</code> and how Kotlin's basic syntax maps to C++.</p>"},{"location":"basics/#packages-and-namespaces","title":"Packages and Namespaces","text":"<p><code>ktox-cpp</code> handles Kotlin packages by transpiling them into C++ namespaces and directory structures.</p> <p>Kotlin: <pre><code>package com.example.math\n\nfun add(a: Int, b: Int): Int = a + b\n</code></pre></p> <p>C++ (transpiled as <code>math/Calculator.hpp</code>): <pre><code>namespace com::example::math {\n\nint32_t add(int32_t a, int32_t b) {\n    return a + b;\n}\n\n}\n</code></pre></p>"},{"location":"basics/#variables-val-vs-var","title":"Variables (<code>val</code> vs <code>var</code>)","text":"<p>Kotlin's distinction between immutable (<code>val</code>) and mutable (<code>var</code>) variables is preserved. <code>val</code> becomes <code>const auto</code> and <code>var</code> becomes <code>auto</code>.</p> <p>Kotlin: <pre><code>val name = \"Kotlin\"      // Immutable\nvar version = 1          // Mutable\nversion += 1\n</code></pre></p> <p>C++: <pre><code>const auto name = \"Kotlin\";\nauto version = 1;\nversion = version + 1;\n</code></pre></p>"},{"location":"basics/#basic-types","title":"Basic Types","text":"<p>Kotlin types are mapped to their standard C++ equivalents from <code>&lt;cstdint&gt;</code> and <code>std</code>.</p> Kotlin Type C++ Type Notes <code>Int</code> <code>int32_t</code> <code>Long</code> <code>int64_t</code> <code>Float</code> <code>float</code> <code>Double</code> <code>double</code> <code>String</code> <code>std::string</code> <code>Boolean</code> <code>bool</code> <code>List</code>, <code>Map</code> <code>std::vector</code>, <code>std::unordered_map</code> From <code>&lt;vector&gt;</code> and <code>&lt;unordered_map&gt;</code> <code>null</code> <code>std::nullopt</code> Uses <code>std::optional</code> for nullable types"},{"location":"basics/#functions","title":"Functions","text":"<p>Top-level functions in Kotlin become free functions within the appropriate namespace in C++.</p> <p>Kotlin: <pre><code>fun greet(name: String): String {\n    return \"Hello, $name!\"\n}\n\nprivate fun internalCalc(x: Int): Int = x * 2\n</code></pre></p> <p>C++: <pre><code>std::string greet(std::string name) {\n    return \"Hello, \" + ktox::toString(name) + \"!\";\n}\n\n// private visibility is handled at the transpilation level\nint32_t internalCalc(int32_t x) {\n    return x * 2;\n}\n</code></pre></p>"},{"location":"basics/#string-interpolation-templates","title":"String Interpolation &amp; Templates","text":"<p>Kotlin's string interpolation is transpiled into C++ string concatenation using <code>ktox::toString()</code> for non-string types. Note that the transpiler often wraps parts in empty strings to ensure correct <code>std::string</code> concatenation.</p> <p>Kotlin: <pre><code>val count = 5\nval message = \"The count is $count\"\nval lang = \"Lua\"\nval msg2 = \"Running on $lang ${version + 0}\"\n</code></pre></p> <p>C++: <pre><code>const auto count = 5;\nconst auto message = \"The count is \" + ktox::toString(count) + \"\";\nconst auto lang = \"Lua\";\nconst auto msg2 = \"Running on \" + ktox::toString(lang) + \" \" + ktox::toString(version + 0) + \"\";\n</code></pre></p>"},{"location":"basics/#control-flow","title":"Control Flow","text":""},{"location":"basics/#if-expressions","title":"If Expressions","text":"<p>In Kotlin, <code>if</code> is an expression. <code>ktox-cpp</code> transpiles simple <code>if</code> expressions to the C++ ternary operator or an Immediately Invoked Lambda Expression (IILE) for complex blocks.</p> <p>Kotlin: <pre><code>val result = if (sum == 15) sum + x else 0\n</code></pre></p> <p>C++: <pre><code>const auto result = (sum == 15 ? sum + x : 0);\n</code></pre></p>"},{"location":"basics/#when-expressions","title":"When Expressions","text":"<p>Kotlin's <code>when</code> expression is transpiled into an <code>if-else</code> chain, often wrapped in an IILE if used as an expression.</p> <p>Kotlin: <pre><code>val color = when (id) {\n    1 -&gt; \"Red\"\n    2 -&gt; \"Green\"\n    else -&gt; \"Unknown\"\n}\n</code></pre></p> <p>C++: <pre><code>const auto color = [&amp;]() {\n    if (id == 1) return \"Red\";\n    else if (id == 2) return \"Green\";\n    else return \"Unknown\";\n}();\n</code></pre></p>"},{"location":"basics/#for-loops-and-ranges","title":"For Loops and Ranges","text":"<p><code>ktox-cpp</code> optimizes Kotlin ranges into efficient C++ for loops. Note that it explicitly uses <code>+= 1</code> or <code>-= 1</code> for increments.</p> <p>Kotlin: <pre><code>for (i in 1..5) {\n    sum += i\n}\n\nfor (i in 0 until 5) {\n    println(i)\n}\n\nfor (i in 5 downTo 1 step 2) {\n    println(i)\n}\n</code></pre></p> <p>C++: <pre><code>for (auto i = 1; i &lt;= 5; i += 1) {\n    sum = sum + i;\n}\n\nfor (auto i = 0; i &lt; 5; i += 1) {\n    println(i);\n}\n\nfor (auto i = 5; i &gt;= 1; i -= 2) {\n    println(i);\n}\n</code></pre></p>"},{"location":"basics/#while-loops","title":"While Loops","text":"<p><code>while</code> and <code>do-while</code> loops map directly to their C++ equivalents.</p> <p>Kotlin: <pre><code>while (x &gt; 5) {\n    x -= 1\n}\n</code></pre></p> <p>C++: <pre><code>while (x &gt; 5) {\n    x = x - 1;\n}\n</code></pre></p>"},{"location":"classes/","title":"Language Guide: Classes &amp; Objects","text":"<p>This section covers how Kotlin classes, interfaces, and objects are transpiled to C++.</p>"},{"location":"classes/#classes","title":"Classes","text":"<p>Kotlin classes are transpiled to C++ <code>class</code> declarations. Primary constructor parameters with <code>val</code> or <code>var</code> become public fields.</p> <p>Kotlin: <pre><code>class Person(val name: String, var age: Int) {\n    fun greet() {\n        println(\"Hello, $name\")\n    }\n}\n</code></pre></p> <p>C++: <pre><code>class Person {\npublic:\n    std::string name;\n    int32_t age;\n\n    Person(std::string name, int32_t age) : name(name), age(age) {}\n\n    void greet() {\n        // println helper from ktox-lib.hpp\n        println(\"Hello, \" + ktox::toString(name) + \"\");\n    }\n};\n</code></pre></p>"},{"location":"classes/#inheritance-interfaces","title":"Inheritance &amp; Interfaces","text":"<p><code>ktox-cpp</code> supports single inheritance and multiple interfaces, mapping them to C++ <code>public</code> inheritance.</p> <p>Kotlin: <pre><code>interface Drawable {\n    fun draw()\n}\n\nopen class Shape(val color: String)\n\nclass Circle(color: String, val radius: Double) : Shape(color), Drawable {\n    override fun draw() {\n        println(\"Drawing a $color circle\")\n    }\n}\n</code></pre></p> <p>C++: <pre><code>class Drawable {\npublic:\n    virtual ~Drawable() = default;\n    virtual void draw() = 0;\n};\n\nclass Shape {\npublic:\n    std::string color;\n    Shape(std::string color) : color(color) {}\n    virtual ~Shape() = default;\n};\n\nclass Circle : public Shape, public Drawable {\npublic:\n    double radius;\n\n    Circle(std::string color, double radius) \n        : Shape(color), radius(radius) {}\n\n    void draw() override {\n        println(\"Drawing a \" + ktox::toString(color) + \" circle\");\n    }\n};\n</code></pre></p>"},{"location":"classes/#objects-and-companion-objects","title":"Objects and Companion Objects","text":"<p>Kotlin <code>object</code> declarations and <code>companion object</code>s are transpiled to C++ classes with static members or as singletons.</p>"},{"location":"classes/#companion-objects","title":"Companion Objects","text":"<p>Companion objects are transpiled to <code>static</code> members within the containing C++ class. Calls use the scope resolution operator <code>::</code>.</p> <p>Kotlin: <pre><code>class Config(val host: String) {\n    companion object {\n        val DEFAULT_PORT = 8080\n        fun createDefault() = Config(\"localhost\")\n    }\n}\n</code></pre></p> <p>C++: <pre><code>class Config {\npublic:\n    std::string host;\n    Config(std::string host) : host(host) {}\n\n    static constexpr auto DEFAULT_PORT = 8080;\n    static Config createDefault() {\n        return Config(\"localhost\");\n    }\n};\n\n// Usage:\nconst auto cfg = Config::createDefault();\n</code></pre></p>"},{"location":"classes/#object-declarations","title":"Object Declarations","text":"<p>Kotlin <code>object</code> declarations are transpiled to Meyer's singletons in C++.</p> <p>Kotlin: <pre><code>object Database {\n    val name = \"Production\"\n    fun connect() { /* ... */ }\n}\n</code></pre></p> <p>C++: <pre><code>class Database {\npublic:\n    static Database&amp; instance() {\n        static Database inst;\n        return inst;\n    }\n\n    std::string name = \"Production\";\n    void connect() { /* ... */ }\n\nprivate:\n    Database() = default;\n};\n</code></pre></p>"},{"location":"classes/#data-classes","title":"Data Classes","text":"<p><code>data class</code> declarations generate standard C++ boilerplate like <code>operator==</code> and <code>toString()</code>.</p> <p>Kotlin: <pre><code>data class Point(val x: Int, val y: Int)\n</code></pre></p> <p>C++: <pre><code>class Point {\npublic:\n    int32_t x;\n    int32_t y;\n\n    Point(int32_t x, int32_t y) : x(x), y(y) {}\n\n    bool operator==(const Point&amp; other) const {\n        return x == other.x &amp;&amp; y == other.y;\n    }\n\n    std::string toString() const {\n        return \"Point(x=\" + ktox::toString(x) + \", y=\" + ktox::toString(y) + \")\";\n    }\n};\n</code></pre></p>"},{"location":"classes/#operator-overloading","title":"Operator Overloading","text":"<p><code>ktox-cpp</code> maps Kotlin <code>operator</code> functions to C++ operator overloads.</p> <p>Kotlin: <pre><code>class Vec2(val x: Float, val y: Float) {\n    operator fun plus(other: Vec2) = Vec2(x + other.x, y + other.y)\n}\n</code></pre></p> <p>C++: <pre><code>class Vec2 {\npublic:\n    float x;\n    float y;\n\n    Vec2(float x, float y) : x(x), y(y) {}\n\n    Vec2 plus(Vec2 other) {\n        return Vec2(x + other.x, y + other.y);\n    }\n};\n\ninline Vec2 operator+(Vec2 a, Vec2 b) {\n    return a.plus(b);\n}\n</code></pre></p>"},{"location":"usage/","title":"Usage &amp; Installation","text":"<p><code>ktox-cpp</code> is distributed as a Gradle plugin. This guide will help you set it up in your Kotlin project.</p>"},{"location":"usage/#installation","title":"Installation","text":"<p>Add the following to your <code>build.gradle.kts</code> file:</p>"},{"location":"usage/#1-apply-the-plugin","title":"1. Apply the Plugin","text":"<pre><code>plugins {\n    id(\"com.isycat.ktox.cpp\") version \"1.0.0\"\n}\n</code></pre>"},{"location":"usage/#2-configure-the-transpiler","title":"2. Configure the Transpiler","text":"<p>Use the <code>kotlinToCpp</code> block to configure the transpiler. All options use standard Kotlin DSL assignment syntax.</p> <p>Minimal: <pre><code>kotlinToCpp {\n    // Directory where generated .hpp files are written. \n    // Defaults to &lt;buildDir&gt;/generated/cpp\n    outputDirectory = layout.projectDirectory.dir(\"outputDir/include\")\n}\n</code></pre></p> <p>Full: <pre><code>kotlinToCpp {\n    // Directory where generated .hpp files are written. \n    // Defaults to &lt;buildDir&gt;/generated/cpp\n    outputDirectory = layout.projectDirectory.dir(\"outputDir/include\")\n\n    // Root package namespace. Strips this prefix from output paths.\n    // Example: \"com.example.app\" -&gt; generated files start at \"app/\"\n    rootNamespace = \"com.example\"\n\n    // Optional list of package prefixes to include (empty = all).\n    includePackages = listOf(\"com.example.features\")\n\n    // Fails the build if Kotlin compilation has errors. Defaults to true.\n    failOnKotlinErrors = true\n\n    // Writes a `mappings.yaml` source-map for offline debugging tools.\n    writeMappingsYaml = false\n}\n</code></pre></p>"},{"location":"usage/#project-structure","title":"Project Structure","text":"<p>Typically, your Kotlin source files should be located in <code>src/main/kotlin</code>. The plugin will mirror the package structure in the specified <code>outputDir</code>.</p> <p>For example, <code>com.example.features.ControlFlowDemo.kt</code> will be transpiled to <code>outputDir/include/features/ControlFlowDemo.hpp</code>.</p>"},{"location":"usage/#gradle-tasks","title":"Gradle Tasks","text":"<p>The plugin adds several tasks to your Gradle project:</p> Task Description Group <code>transpileKotlinToCpp</code> Transpiles Kotlin sources to C++ headers. Wired into <code>assemble</code>. <code>build</code> <code>checkCppOutput</code> Syntax-checks every emitted <code>.hpp</code> with <code>-fsyntax-only</code> (skips when no <code>g++</code>/<code>clang++</code> is on the PATH). Wired into <code>check</code>. <code>verification</code>"},{"location":"usage/#runtime-support","title":"Runtime Support","text":"<p>All transpiled code requires <code>ktox-lib.hpp</code> to be available in your C++ project's include path. The plugin automatically provides this library (it is usually shipped with the plugin or generated during the build).</p> <pre><code>#include \"ktox-lib.hpp\"\n</code></pre>"}]}